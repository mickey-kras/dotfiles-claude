{{- $home := replace "\\" "/" .chezmoi.homeDir -}}
{{- $runtime := includeTemplate "templates/resolved-state.json" . | fromJson -}}
{{- $resolved := $runtime.resolved -}}
{{- $selectedMcps := index $resolved "mcps" "enabled" -}}
{{- $settings := index $resolved "settings" -}}
{
  "mcpServers": {
{{- if has "playwright" $selectedMcps }}
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@0.0.68"]
    }{{ if or (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "atlassian" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "context7" $selectedMcps }}
    "context7": {
      "url": "https://mcp.context7.com/mcp"
    }{{ if or (has "figma" $selectedMcps) (has "atlassian" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "figma" $selectedMcps }}
    "figma": {
      "url": "https://mcp.figma.com/mcp"
    }{{ if or (has "atlassian" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "atlassian" $selectedMcps }}
    "atlassian": {
      "url": "https://mcp.atlassian.com/v1/mcp"
    }{{ if or (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "filesystem" $selectedMcps }}
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem@2026.1.14", "{{ $home }}/Dev"]
    }{{ if or (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "git" $selectedMcps }}
    "git": {
      "command": "npx",
      "args": ["-y", "@cyanheads/git-mcp-server@2.10.5"]
    }{{ if or (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "shell" $selectedMcps }}
    "shell": {
      "command": "npx",
      "args": ["-y", "super-shell-mcp@2.0.15"]
    }{{ if or (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "terraform" $selectedMcps }}
    "terraform": {
      "command": "npx",
      "args": ["-y", "terraform-mcp-server@0.13.0"]
    }{{ if or (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "kubernetes" $selectedMcps }}
    "kubernetes": {
      "command": "npx",
      "args": ["-y", "mcp-server-kubernetes@3.4.0"]
    }{{ if or (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "process" $selectedMcps }}
    "process": {
      "command": "npx",
      "args": ["-y", "@ai-capabilities-suite/mcp-process@1.5.10"]
    }{{ if or (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "thinking" $selectedMcps }}
    "thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking@2025.12.18"]
    }{{ if or (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) (and (has "docker" $selectedMcps) (lookPath "docker")) }},{{ end }}
{{- end }}
{{- if has "memory" $selectedMcps }}
    "memory": {
{{- if and (eq (index $settings "memory_provider") "obsidian") (index $settings "obsidian_vault_path") }}
      "command": "npx",
      "args": ["-y", "@bitbonsai/mcpvault@latest", {{ index $settings "obsidian_vault_path" | toJson }}]
{{- else }}
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory@2026.1.26"]
{{- end }}
    }
{{- end }}
{{- if and (has "http" $selectedMcps) (lookPath "uvx") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) }},{{ end }}
    "http": {
      "command": "uvx",
      "args": ["mcp-server-fetch==2025.4.7"]
    }
{{- end }}
{{- if and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) }},{{ end }}
    "azure-devops": {
      "command": "npx",
      "args": ["-y", "@azure-devops/mcp@2.5.0", "{{ index $settings "azure_devops_org" }}"]
    }
{{- end }}
{{- if and (has "github" $selectedMcps) (lookPath "bw") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) }},{{ end }}
    "github": {
      "command": "node",
      "args": ["{{ $home }}/.local/bin/bw-mcp", "mcp-github", "GITHUB_PERSONAL_ACCESS_TOKEN=password", "--", "npx", "-y", "@modelcontextprotocol/server-github@2025.4.8"]
    }
{{- end }}
{{- if and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) }},{{ end }}
    "aws": {
      "command": "node",
      "args": ["{{ $home }}/.local/bin/bw-mcp", "mcp-aws", "AWS_ACCESS_KEY_ID=AWS_ACCESS_KEY_ID,AWS_SECRET_ACCESS_KEY=AWS_SECRET_ACCESS_KEY", "--", "uvx", "awslabs.aws-api-mcp-server==1.3.26"]
    }
{{- end }}
{{- if and (has "tailscale" $selectedMcps) (lookPath "bw") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) }},{{ end }}
    "tailscale": {
      "command": "node",
      "args": ["{{ $home }}/.local/bin/bw-mcp", "mcp-tailscale", "TAILSCALE_API_KEY=password,TAILSCALE_TAILNET=TAILSCALE_TAILNET", "--", "npx", "-y", "@hexsleeves/tailscale-mcp-server@0.3.2"]
    }
{{- end }}
{{- if and (has "exa" $selectedMcps) (lookPath "bw") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) }},{{ end }}
    "exa": {
      "command": "node",
      "args": ["{{ $home }}/.local/bin/bw-mcp", "exa-api-key", "EXA_API_KEY=password", "--", "npx", "-y", "exa-mcp-server@3.1.9"]
    }
{{- end }}
{{- if and (has "firecrawl" $selectedMcps) (lookPath "bw") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) }},{{ end }}
    "firecrawl": {
      "command": "node",
      "args": ["{{ $home }}/.local/bin/bw-mcp", "firecrawl-api-key", "FIRECRAWL_API_KEY=password", "--", "npx", "-y", "firecrawl-mcp@3.11.0"]
    }
{{- end }}
{{- if and (has "fal-ai" $selectedMcps) (lookPath "bw") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) }},{{ end }}
    "fal-ai": {
      "command": "node",
      "args": ["{{ $home }}/.local/bin/bw-mcp", "fal-api-key", "FAL_KEY=password", "--", "npx", "-y", "fal-ai-mcp-server@2.1.4"]
    }
{{- end }}
{{- if and (has "docker" $selectedMcps) (lookPath "docker") }}{{ if or (has "playwright" $selectedMcps) (has "context7" $selectedMcps) (has "figma" $selectedMcps) (has "filesystem" $selectedMcps) (has "git" $selectedMcps) (has "shell" $selectedMcps) (has "terraform" $selectedMcps) (has "kubernetes" $selectedMcps) (has "process" $selectedMcps) (has "thinking" $selectedMcps) (has "memory" $selectedMcps) (and (has "http" $selectedMcps) (lookPath "uvx")) (and (has "azure-devops" $selectedMcps) (index $settings "azure_devops_org")) (and (has "github" $selectedMcps) (lookPath "bw")) (and (has "aws" $selectedMcps) (lookPath "bw") (lookPath "uvx")) (and (has "tailscale" $selectedMcps) (lookPath "bw")) (and (has "exa" $selectedMcps) (lookPath "bw")) (and (has "firecrawl" $selectedMcps) (lookPath "bw")) (and (has "fal-ai" $selectedMcps) (lookPath "bw")) }},{{ end }}
    "MCP_DOCKER": {
      "command": "docker",
      "args": ["mcp", "gateway", "run"]
    }
{{- end }}
  }
}
