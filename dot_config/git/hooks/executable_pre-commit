#!/usr/bin/env bash

set -euo pipefail

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_root=$(git rev-parse --show-toplevel)

"$script_dir/_misha_git_policy.py" check-staged --repo-root "$repo_root"
